.Hero {
  margin-top: -160px;
  padding: 18px 16px;

  &-Item {
    position: relative;
  }

  &-Title {
    position: absolute;
    top: 180px;
    left: 0;
    max-width: 40%;
    margin-top: 40px;
    margin-bottom: 157px;
  }

  &-Text {
    margin-top: 26px;
    margin-bottom: 60px;
    font-size: 18px;
    line-height: 27px;
    color: var(--color-text-main);
  }
}
